services:
  - type: web
    name: code-relay-platform
    env: node
    plan: free # Change to 'starter' if using a Disk for persistence
    buildCommand: npm install && npm run build
    startCommand: npx prisma migrate deploy && npm start
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
      - key: PRISMA_CLIENT_ENGINE_TYPE
        value: library
      - fromGroup: database-secrets

envVarGroups:
  - name: database-secrets
    envVars:
      - key: DATABASE_URL
        value: postgresql://coderelay_user:me4WJmVIymEQlp79EbTED0yjqdqzBhiI@dpg-d64rslshg0os73df80g0-a.singapore-postgres.render.com/coderelay?sslmode=require
